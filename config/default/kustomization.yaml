apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Everything is deployed into this namespace and prefixed for clarity.
namespace: proof-of-deploy-system
namePrefix: proof-of-deploy-

resources:
  - ../rbac
  - ../manager
