apiVersion: v1
kind: ServiceAccount
metadata:
  name: controller-manager
  namespace: system
  labels:
    app.kubernetes.io/name: proof-of-deploy
  # For AWS KMS access, annotate this ServiceAccount with an IRSA role ARN, e.g.
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<acct>:role/proof-of-deploy-signer
